// time to ms then display in min hours and secs using calculations
// we use `${hrs}:${min}:${sec}`
// mode :Focusession, shortBreak, LongBreak
// default value 25 should be on display for session and 5, 10 for shortBreak and longBreak respectively
// use inertval to run the secs and if the interval count>60 we reduse mins by one
// 25*60*100
// removeInterval case >= 25*60
// from the below calculation will update display timer
// totalTime=25*60   =1500(mins->sec)
// hrs= 25/60
// min=25%60
// console.log('hours::',Math.floor(25/60))
// console.log('mins::',Math.abs(25%60))
// will run the interval till the 60 sec and keep the counter if the counter exceeds 60 then minus minutes by one





